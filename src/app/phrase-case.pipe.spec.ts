import { PhraseCasePipe } from './phrase-case.pipe';

describe('PhraseCasePipe', () => {
  it('create an instance', () => {
    const pipe = new PhraseCasePipe();
    expect(pipe).toBeTruthy();
  });
});
